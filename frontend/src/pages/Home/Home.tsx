import { FC } from 'react';
import CommonLayout from '../../components/ui/layout/CommonLayout';

interface HomeProps {}

const Home: FC<HomeProps> = ({}) => {
  return <CommonLayout>Home</CommonLayout>;
};

export default Home;
